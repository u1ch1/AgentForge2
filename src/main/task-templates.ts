/**
 * Типовые планы для частых на Kwork/FL.ru категорий задач — не готовый JSON
 * плана (Admin всё равно должен сам решить конкретные файлы и детали под
 * задачу), а проверенная на практике разбивка на подзадачи, которую Admin
 * адаптирует вместо того чтобы каждый раз изобретать её заново. Экономит и
 * время, и токены на обсуждении структуры плана.
 *
 * Подбирается по ключевым словам в самой цели (goal) — см. matchTaskTemplate()
 * и её вызов в doPlanning() (orchestrator.ts). Если ни один шаблон не подошёл,
 * Admin планирует как раньше, без подсказки — это чистое дополнение, не
 * ограничение.
 */

export interface TaskTemplate {
  name: string
  match: RegExp
  outline: string
}

export const TASK_TEMPLATES: TaskTemplate[] = [
  {
    name: 'WordPress тема / плагин',
    // Стоит первым в списке намеренно: "тема WordPress для лендинга" или
    // "интернет-магазин на WordPress" совпадёт и с более общим шаблоном
    // ниже (Лендинг, Интернет-магазин), а matchTaskTemplate() берёт первое
    // совпадение по порядку — но именно WordPress здесь определяет и стек,
    // и структуру файлов, и то, что живой проверки не будет, так что этот
    // сигнал важнее общего типа контента.
    match: /wordpress|wp-тема|wp-плагин|вордпресс/i,
    outline: `## Типовой план — WordPress тема/плагин
1. Тема: style.css с заголовком темы (Theme Name и т.д.), functions.php,
   шаблоны по иерархии WordPress (index.php, header.php, footer.php,
   page.php/single.php по задаче) — ЛИБО плагин: один главный файл с
   заголовком плагина (Plugin Name и т.д.) и хуками init/admin_init.
2. Логика на хуках и фильтрах (add_action/add_filter), а не прямым
   изменением чужого кода или ядра WordPress.
3. Экранирование вывода (esc_html/esc_attr/esc_url) и очистка ввода
   (sanitize_text_field и т.п.) на каждом месте, где это уместно —
   без этого Тестер обязан пометить как критическое.
4. Запросы к БД — только через $wpdb->prepare(), не сырой SQL со
   вставкой значений в строку.
Один assignee (backend), без разбивки на frontend/backend. Каркас темы
или плагина не подкладывается заранее — в отличие от Vite/Express, для
WordPress здесь нет ни самого ядра WordPress, ни MySQL, чтобы собрать и
живьём проверить сайт, поэтому Тестер сможет оценить только код по
ревью (и синтаксис PHP, если он установлен), не запуск. Результат —
файлы темы/плагина для установки в уже существующий WordPress
пользователя, а не готовый к показу через «Просмотр» сайт.`,
  },
  {
    name: 'Лендинг / одностраничный сайт',
    match: /лендинг|одностраничник|визитк|landing/i,
    outline: `## Типовой план — лендинг
Обычно 3-5 подзадач, почти весь объём на фронтенде:
1. (frontend) Секции страницы: hero, преимущества/услуги, отзывы или портфолио, форма заявки, футер
2. (frontend) Адаптивная вёрстка под мобильные
3. (frontend) Форма заявки — валидация и отправка
Backend заводи, только если форма должна что-то сохранять или пересылать —
иначе для лендинга он не нужен вовсе.`,
  },
  {
    name: 'Интернет-магазин / каталог',
    match: /магазин|каталог|корзин|интернет-магазин|e-?commerce/i,
    outline: `## Типовой план — интернет-магазин
1. (backend) Модель товара, эндпоинты списка/карточки/категорий
2. (backend) Приём заказа (корзина оформляется на фронтенде, сохраняется на бэкенде)
3. (frontend) Каталог с фильтром по категориям, карточка товара
4. (frontend) Корзина (можно на localStorage) и форма оформления заказа
Для данных о товарах достаточно простого JSON/SQLite, если задача не
просит конкретную БД явно.`,
  },
  {
    name: 'CRUD-админка / панель управления',
    match: /админк|панель управления|dashboard|crud/i,
    outline: `## Типовой план — админ-панель
1. (backend) CRUD-эндпоинты основной сущности (список/создание/правка/удаление)
2. (backend) Простая авторизация, если панель не должна быть открыта всем
3. (frontend) Таблица со списком + форма создания-редактирования
4. (frontend) Подтверждение перед удалением`,
  },
  {
    name: 'Telegram-бот',
    match: /telegram|телеграм.?бот|тг.?бот/i,
    outline: `## Типовой план — Telegram-бот
1. (backend) Инициализация бота и обработка команд (/start и основные сценарии)
2. (backend) Хранение состояния диалога/данных пользователя
3. (backend) Интеграция с внешним API, если бот показывает не только свои данные
Фронтенда у обычного бота нет — не заводи assignee "frontend" без явной
причины (Telegram Mini App — это отдельный, явно оговоренный случай).`,
  },
  {
    name: 'Парсер / скрапер',
    match: /парсер|скрапер|парсинг|scrap(?:er|ing)/i,
    outline: `## Типовой план — парсер/скрапер
1. Получение данных с источника (HTTP-запрос или headless-браузер — смотря что отдаёт сайт)
2. Разбор и нормализация данных
3. Сохранение результата (файл/БД) и, если нужно, уведомление (email/Telegram)
Обычно это один assignee без разбивки на frontend/backend — скрипт, а не
веб-приложение.`,
  },
  {
    name: 'Блог / контентный сайт',
    match: /блог|cms|статьи|новост/i,
    outline: `## Типовой план — блог/контентный сайт
1. (backend) Модель статьи, эндпоинты списка и отдельной статьи
2. (frontend) Список статей и страница отдельной статьи
3. (backend, если нужно) Простая админка для добавления статей`,
  },
]

export function matchTaskTemplate(goal: string): TaskTemplate | null {
  return TASK_TEMPLATES.find((t) => t.match.test(goal)) ?? null
}
